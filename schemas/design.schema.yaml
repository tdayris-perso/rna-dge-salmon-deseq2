$schema: "http://json-schema.org/draft-04/schema#"

description: An entry in the sample sheet

proprerties:
  Sample_id:
    type: string
    description: The sample unique identifier
  salmon:
    type: string
    description: Path to salmon quantification directory

required:
  - Sample_id
  - Salmon
